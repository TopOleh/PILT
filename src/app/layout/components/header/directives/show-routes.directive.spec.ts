import { ShowRoutesDirective } from './show-routes.directive';

describe('ShowRoutesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowRoutesDirective();
    expect(directive).toBeTruthy();
  });
});
