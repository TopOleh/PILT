// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebaseConfig : {
    apiKey: 'AIzaSyDy7AjJXqbSNiNa5AAvRTzl6o_Z8yCifco',
    authDomain: 'play-in-life-together.firebaseapp.com',
    databaseURL: 'https://play-in-life-together.firebaseio.com',
    projectId: 'play-in-life-together',
    storageBucket: 'play-in-life-together.appspot.com',
    messagingSenderId: '642383780542',
  },
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
